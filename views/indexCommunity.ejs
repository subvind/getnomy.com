<!DOCTYPE html>
<html lang="en">
<%- include('head', { title }); %>
<body>
  <%- include('header'); %>
  <%- include('breadcrumb', {
    links: [
      { name: `CMS`, url: `/` },
      { name: `Community`, url: `/` },
      { name: `${community.slug}`, url: `/community/${community.slug}` },
    ]
  }); %>
  <br />
  <br />
  <%- // JSON.stringify(community, null, 2); %>

  <div id="main-result" class="container">
    <%- include('tenants/index', { tenantTable }); %>
  </div>
  <br />
  <%- include('auth/index') %>
  <%- include('footer'); %>

  <!-- Include htmx -->
  <script src="https://unpkg.com/htmx.org@1.7.0/dist/htmx.js"></script>
</body>
</html>
